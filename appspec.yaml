version: 1
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: !Ref VreservAdminTaskDef
        LoadBalancerInfo:
          ContainerName: "vreserv-admin-container"   
          ContainerPort: 3000
